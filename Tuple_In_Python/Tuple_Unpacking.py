# To store elements of a tuple in separate variables is called unpacking.
# This operation allows us to take
# multiple return values from a function and store them in separate variables.
example_tup = (3, 2, 1)
a, b, c = example_tup
print("a:", a, "b:", b, "c:", c)

# Can be used when returning values in function.